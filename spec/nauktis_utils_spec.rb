require 'spec_helper'

describe NauktisUtils do
  it 'has a version number' do
    expect(NauktisUtils::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
